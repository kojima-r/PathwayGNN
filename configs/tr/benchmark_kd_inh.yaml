defaults:
  - dataset.yaml

seed: 42
device: auto

dataset:
  task: kd_inh

folds: 5
n_jobs: -1
logistic_regression: {C: 1.0}
random_forest: {n_estimators: 200, max_depth: null}
xgboost: {enabled: true, n_estimators: 200, max_depth: 4, learning_rate: 0.05, subsample: 0.8}
output_dir: outputs/tr/benchmark/kd_inh
